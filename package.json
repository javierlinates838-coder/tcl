{
  "name": "mobile-detailing-site",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node preview.mjs",
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.mjs"
  }
}
